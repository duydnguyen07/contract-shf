{
  "rules": {
    "contacts": {
      "$contact" : {
        ".validate": "newData.hasChildren(['first_name', 'last_name', 'email']) && newData.child('email').val().length > 0",
        ".write": true
       }
    },
    "$other": {
      ".write": false
    },
    ".read": false,
  }
}
